fragment_downloaded_cb({"url": "Download.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"Download.md\">\n<h1 id=\"downloading-meson\">Downloading Meson</h1>\n<p>Meson releases can be downloaded from the <a href=\"https://github.com/jpakkane/meson/releases\">Github release page</a>.</p>\n<p>Meson is also available in the <a href=\"https://pypi.python.org/pypi/meson/\">Python Package Index</a> and can be\ninstalled with <tt>pip3 install meson</tt>.</p>\n<p>The newest development code can be obtained directly from <a href=\"https://github.com/jpakkane/meson\">Git</a></p>\n<h2 id=\"dependencies\">Dependencies</h2>\n<p>Meson is implemented in Python 3. If your operating system provides a\npackage manager, use should install it with that. For platforms that\ndon't have a package manager, you need to download it from <a href=\"https://www.python.org/downloads/\">Python's\nhome page</a>.</p>\n<p>Depending on your platform and backend you wish to use, you might need\nthe <a href=\"https://ninja-build.org/\">Ninja executable</a>. Again, use your distro-provided version if\npossible. Otherwise download it from Ninja project's web site.</p>\n\n</div>\n\n\n        "});