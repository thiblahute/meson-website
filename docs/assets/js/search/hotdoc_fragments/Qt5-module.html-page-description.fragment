fragment_downloaded_cb({"url": "Qt5-module.html#page-description", "fragment": "<div id=\"page-description\" data-hotdoc-source=\"Qt5-module.md\">\n<h1 id=\"qt5-module\">Qt5 module</h1>\n<p>The Qt5 module provides tools to automatically deal with the various tools and steps required for Qt. The module has one method.</p>\n<h2 id=\"preprocess\">preprocess</h2>\n<p>This method takes four keyword arguments, <code>moc_headers</code>, <code>moc_sources</code>, <code>ui_files</code> and <code>qresources</code> which define the files that require preprocessing with <code>moc</code>, <code>uic</code> and <code>rcc</code>. It returns an opaque object that should be passed to a main build target. A simple example would look like this:</p>\n<pre><code class=\"language-meson\">qt5 = import('qt5')\nqt5_dep = dependency('qt5', ...)\nmoc_files = qt5.preprocess(moc_headers : 'myclass.h')\nexecutable('myprog', 'main.cpp', 'myclass.cpp', moc_files,\n           dependencies : qt5_dep)\n</code></pre>\n\n</div>\n\n\n        "});